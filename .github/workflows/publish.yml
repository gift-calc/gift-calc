name: Publish to NPM

on:
  push:
    branches: [ master ]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write  # Required for npm provenance
      actions: write   # Required to bypass branch protection
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch full history for semantic-release
          token: ${{ secrets.GITHUB_TOKEN }}  # Use GITHUB_TOKEN for checkout
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test
      
      - name: Run semantic-release
        id: semantic
        run: |
          echo "🚀 Running semantic-release..."
          # Configure git for semantic-release
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          # Set remote URL with token for authenticated pushes
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/gift-calc/gift-calc.git
          
          # Run semantic-release
          npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          
      - name: Get release info
        if: always()  # Run even if semantic-release doesn't publish
        id: release_info
        run: |
          # Check if a new version was published by looking for the latest tag
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          PREVIOUS_TAG=$(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || echo "")
          
          if [ "$LATEST_TAG" != "$PREVIOUS_TAG" ] && [ -n "$LATEST_TAG" ]; then
            echo "published=true" >> $GITHUB_OUTPUT
            NEW_VERSION=${LATEST_TAG#v}  # Remove 'v' prefix if present
            echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
            echo "🎉 New version published: $NEW_VERSION"
          else
            echo "published=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No new version published"
          fi
          
          # Check if files affecting website demo changed (regardless of version)
          DEMO_FILES_CHANGED=$(git diff HEAD~1 --name-only | grep -E '^(index\.js|src/|package\.json)' || true)
          if [ -n "$DEMO_FILES_CHANGED" ]; then
            echo "core-changed=true" >> $GITHUB_OUTPUT
            echo "✅ Demo-affecting files changed, will sync to website:"
            echo "$DEMO_FILES_CHANGED"
          else
            echo "core-changed=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No demo-affecting files changed"
          fi
          
      - name: Build web bundle
        if: steps.release_info.outputs.core-changed == 'true'
        run: |
          echo "🌐 Building web bundle..."
          if [ -f src/core-web.js ]; then
            cp src/core-web.js src/core-bundle.js
            echo "✅ Web bundle created from src/core-web.js"
          else
            echo "⚠️ src/core-web.js not found, skipping web bundle creation"
          fi
          
      - name: Update Homebrew formula
        if: steps.release_info.outputs.published == 'true'
        run: |
          echo "🍺 Updating Homebrew formula..."
          NEW_VERSION="${{ steps.release_info.outputs.version }}"
          echo "🔄 Triggering Homebrew update for version $NEW_VERSION"
          
          RESPONSE=$(curl -w "%{http_code}" -s -X POST \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: token ${{ secrets.HOMEBREW_DISPATCH_TOKEN }}" \
            https://api.github.com/repos/gift-calc/homebrew-gift-calc/dispatches \
            -d "{\"event_type\":\"version_update\",\"client_payload\":{\"version\":\"$NEW_VERSION\"}}")
          
          if [ "$RESPONSE" -eq "204" ]; then
            echo "✅ Homebrew update triggered successfully"
          else
            echo "⚠️ Homebrew update request returned status: $RESPONSE"
          fi
          
      - name: Sync core to website
        if: steps.release_info.outputs.core-changed == 'true'
        run: |
          echo "🌍 Syncing core logic and terminal to website..."
          
          # Get current version from package.json  
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "📦 Current version: $CURRENT_VERSION"
          
          # Clone the website repository
          echo "📥 Cloning website repository..."
          git clone https://${{ secrets.GITHUB_TOKEN }}@github.com/gift-calc/gift-calc.github.io.git website-repo
          
          # Create js directory if it doesn't exist
          mkdir -p website-repo/js
          
          # Copy the web-compatible core if it exists
          if [ -f src/core-web.js ]; then
            cp src/core-web.js website-repo/js/core.js
            echo "📋 Copied core-web.js to website/js/core.js"
          else
            echo "⚠️ src/core-web.js not found, skipping core sync"
          fi
          
          # Process terminal template and copy to website
          if [ -f src/terminal-web.js ]; then
            # Replace {{VERSION}} placeholder with actual version
            sed "s/{{VERSION}}/$CURRENT_VERSION/g" src/terminal-web.js > website-repo/js/terminal.js
            echo "📋 Processed terminal-web.js template and copied to website/js/terminal.js"
            echo "🔄 Version $CURRENT_VERSION injected into terminal.js"
          else
            echo "⚠️ src/terminal-web.js not found, skipping terminal sync"
          fi
          
          # Configure git for website repo
          cd website-repo
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Check for any changes and commit
          CHANGED_FILES=$(git status --porcelain js/)
          if [ -n "$CHANGED_FILES" ]; then
            echo "📝 Changed files detected:"
            echo "$CHANGED_FILES"
            
            git add js/
            git commit -m "chore: sync core logic and terminal from main repository (v$CURRENT_VERSION)"
            git push origin master
            echo "✅ Website updated with new core logic and terminal"
          else
            echo "ℹ️ No changes detected, website already up to date"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}